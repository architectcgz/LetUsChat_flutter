class UnreadMessage {
  String contactId;
  int unreadCount;
  String lastMessage;
  UnreadMessage(
      {required this.contactId,
      required this.unreadCount,
      required this.lastMessage});
}
